# Flow Ad Assets
Temporary public image hosting used by social_publish.py during Instagram/Facebook publishing. Images are pushed here briefly so Meta's Graph API can fetch a public URL, then removed after the post is live.
